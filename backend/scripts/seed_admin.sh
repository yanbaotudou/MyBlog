#!/usr/bin/env bash
set -euo pipefail

echo "Admin seeding is handled automatically when backend starts."
echo "Set ADMIN_SEED_USERNAME and ADMIN_SEED_PASSWORD before startup."
echo "After first startup, please log in and change the default admin password immediately."
